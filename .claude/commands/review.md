# /review — Spaced review session

Read `AGENTS.md` in the repo root and run a review exactly as defined in § Review session:
overdue SRS topics first, mandatory gaps included, 3–5 rephrased retrieval questions,
then all SRS/gap/tracker updates per § After each session.

`AGENTS.md` is the single source of truth — this command only triggers it.
