# /learn — Start the next learning session

Time budget argument: "$ARGUMENTS" (may be empty).

1. Read `AGENTS.md` in the repo root — it defines the full coaching protocol. Follow it exactly.
2. Fix the time budget (§ Time budget): if the argument is empty, ask S/M/L; otherwise map it (≤ 15 → S · 16–39 → M · ≥ 40 → L, or s/m/l directly).
3. Select the session via § Auto-rotation logic (paused check → review cap → SRS check → rotation).
4. Run the session including warm-up and all output/tracking rules (§ After each session).

`AGENTS.md` is the single source of truth — this command only triggers it. Never re-implement or shortcut its rules here.
